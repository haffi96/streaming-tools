[project]
name = "livekit-tester"
version = "0.1.0"
description = "LiveKit tester for network video/data streaming"
requires-python = ">=3.10"
dependencies = [
    "livekit>=1.1.5",
]

[dependencies]

[dependency-groups]
dev = [
    "ruff>=0.14.14",
]
